# Використовуємо офіційний образ Nginx
FROM nginx:alpine

# Копіюємо вміст поточної директорії в директорію Nginx для сервінгу
COPY . /usr/share/nginx/html

# Виставляємо порт 80 для доступу до веб-сервера
EXPOSE 80
